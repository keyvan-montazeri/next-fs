# Example of AI Agent using Freeswitch and GPT4All
I will trying to write a simple dockerized stack using followed tech:

* GPT4All
* FreeSwitch
* NextJS for interface and managing
* fastapi to get LLM responces

``` bash
docker compose up -d
docker compose down -v

```